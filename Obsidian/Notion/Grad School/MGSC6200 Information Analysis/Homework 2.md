**Chapter 9**

4. a.) The best chance of tripling your money is to go with the randomly chosen stocks. This is because index funds have stable returns, and the odds of getting a 3x return in a month are essentially zero.

b.) The riskiest move is also to go with randomly chosen stocks. Again, for the same reason. The index funds are stable, and grow slowly over time. They have such a large number of investments that it is unlikely that any singularly bad stock will dramatically alter the fund.

22.a.) Since we do not know the true error, I will use 50% to give a worst case scenario. In this case the standard error is: sqrt((true%*(100-true%)/sample size)), which in this case is 1.6%. The margin of error is therefore 3.2%. The standard error is the variability in the sample, whereas margin of error is the largest reasonable gap (i.e. 1.6 on either side of the number)

b.) A sample size of 100 will yield a 10% margin of error (calculated in the same method as above).

c.) I would expect to get around 2%. This is due to the fact that the standard error for a sample percentage estimates the standard deviation of a list od sample percentages taken from repeated samples (in our case 25 groups of 1000) (textbook pg 317)

28. For an SE of 0.5%, we use the formula: SEavg=SD/sqrt(sample size). This allows for us to input: sample size= (SD/SE)^2

which equals: (2.5/.05)^2 = **2500**

  

**Chapter 10**

17.a.) SE=sd/(sqrt(samplesize) therefore SE=2.1/sqrt(1100)=.06 hrs, so ME=.12, which meands the 95% interval is 4.8+/-0.12 hrs

b)this means that we can correctly state with 95% confidence that 4.8+/-0.12 hours are spent at work on non-job reslated site.

c) 110 expected vs 145 actual means this company is within normal distribution of the sample, albeit on the high side.

d) 4.2 represents 95% of the value, so it is not correct to use that.

20.a) SE=sqrt((90*10)/400) therefor 95% that 3% will not show, 87 and 93% will show.

b)X+-(.90)((1.5*sqrt400)/sqrt(400))= 1.35% therefore they should sell 404 seats

28. I am confused by this question, but I will give it a go:

a) sample size is now 1

b) appropriate calculation of confidence interval without recorded data from smaller sample, but using smaller sample size in calculation which is not correct

c) appropriately calculated from smaller sample

d) using incorrect sd for sample as gathered.

  

**Chapter 11**

11.se=sd/sqrtsample size therefore se= 2/sqrt(2500) ~4% which equates to 4 days

13. They have calculated an estimated z value based on the p-value. I

X. This represents 59.7% of that population. The sample size is barely large enough to dispute those findings. sqrt((47*53)/67) is around 6%, so +-12 is allowed. this leads us to 59% vs 59.7%, so It is reasonable that more data should be collected due to the fact that this represents statistically significant information, but just barely.